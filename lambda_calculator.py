add = lambda x,y: x + y
subtract = lambda x,y: x-y
multiply = lambda x,y: x*y
quotient = lambda x,y: x / y

if __name__ == "__main__":
    x = 10
    y = 5
    print(add(x, y))
    print(subtract(x,y))
    print(multiply(x,y))
    print(quotient(x,y))